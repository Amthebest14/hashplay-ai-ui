require("dotenv").config({ path: "../.env" }); const { ethers } = require("ethers"); async function main() { const provider = new ethers.JsonRpcProvider("https://mainnet.hashio.io/api"); const feeData = await provider.getFeeData(); const gasLimit = 1200000n; const costInWei = gasLimit * feeData.gasPrice; console.log("Gas Price (Wei):", feeData.gasPrice.toString()); console.log("Total Cost (Wei):", costInWei.toString()); console.log("Total Cost (HBAR):", ethers.formatUnits(costInWei, 18) * 1e10); } main();
