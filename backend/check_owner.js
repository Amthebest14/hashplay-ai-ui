require("dotenv").config({ path: "../.env" }); const { ethers } = require("ethers"); async function main() { const provider = new ethers.JsonRpcProvider("https://mainnet.hashio.io/api"); const abi = ["function owner() external view returns (address)"]; const contract = new ethers.Contract(process.env.VITE_MINING_ENGINE_ADDRESS, abi, provider); const owner = await contract.owner(); console.log("Owner is:", owner); } main();
